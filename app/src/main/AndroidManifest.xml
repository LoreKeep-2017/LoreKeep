<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ilya.lorekeep">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".topic.TopicActivity"
            android:label="@string/title_activity_topics"/>
        <activity android:name=".topic.NewTopicActivity"
                  android:label="NewTopicctivity"/>
        <activity
            android:name=".note.NoteActivity"
            android:label="@string/title_activity_links"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.vk.sdk.VKServiceActivity"
            android:label="ServiceActivity"
            android:theme="@style/VK.Transparent" />
        <activity android:name=".note.ListNoteActivity" />
        <activity android:name=".note.CreateNoteActivity"></activity>
    </application>

</manifest>